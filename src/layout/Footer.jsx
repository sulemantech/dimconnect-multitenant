export default () => {
    return (
        <div className="relative flex flex-col flex-1 bg-components text-xs py-2 text-white border-t-2 border-white border-solid mt-2">
            {new Date().getFullYear() + " © " + " TUV"}
        </div>
    )
}